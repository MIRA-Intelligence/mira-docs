services:
  mira-docs:
    image: nginx:1.27-alpine
    container_name: mira-docs
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - /opt/mira-docs/site:/usr/share/nginx/html:ro
      - /opt/mira-docs/nginx.conf:/etc/nginx/conf.d/default.conf:ro
